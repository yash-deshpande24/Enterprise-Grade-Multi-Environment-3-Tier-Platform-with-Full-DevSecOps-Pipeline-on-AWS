version: "3.9"

services:
  web:
    build: ./app/web
    ports:
      - "8081:80"
    environment:
      - APP_API_URL=http://api:8080
    depends_on:
      - api

  api:
    build: ./app/api
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NODE_ENV=dev
